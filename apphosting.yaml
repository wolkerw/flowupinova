# Settings for Backend (on Cloud Run).
# See https://firebase.google.com/docs/app-hosting/configure#cloud-run
runConfig:
  minInstances: 0
  # maxInstances: 100
  # concurrency: 80
  # cpu: 1
  # memoryMiB: 512

# Environment variables and secrets.
env:
  - variable: NEXT_PUBLIC_DEMO_VIDEO_URL
    value: "https://firebasestorage.googleapis.com/v0/b/studio-7502195980-3983c.firebasestorage.app/o/videos%2Fapresentacao-numvapt.mp4.mp4?alt=media&token=be12c927-04ba-4387-bac8-4a129f2d9435"
    availability:
      - BUILD
      - RUNTIME

# Grant access to secrets in Cloud Secret Manager.
# You can create secrets with the CLI command `firebase apphosting:secrets:set <secretName>`.
# See https://firebase.google.com/docs/app-hosting/configure#secret-parameters
# - variable: MY_SECRET
#   secret: mySecretRef
